# Examples

The supported example path is:

- [`portfolio_demo`](./portfolio_demo)

Everything under [`legacy`](./legacy) is archived context from earlier, broader iterations of the repository. Those examples are not part of the current public contract and should not be used as setup or product guidance.
