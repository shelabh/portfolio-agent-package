# Legacy API Modules

This directory contains archived API surfaces from earlier repository iterations.

They are intentionally not mounted by `create_app(...)` and are not part of the supported SDK-first contract.
